server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Serve SPA — fall back to index.html for any unknown path
    location / {
        try_files $uri $uri/ /index.html;
    }
}